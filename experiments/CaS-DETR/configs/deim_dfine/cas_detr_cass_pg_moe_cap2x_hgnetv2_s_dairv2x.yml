__include__: [
  './cas_detr_hgnetv2_s_dairv2x.yml'
]

# Explicit CASS naming for the compute-matched PG-MoE baseline.
output_dir: ./outputs/dairv2x/main/cas_detr_cass_pg_moe_cap2x_hgnetv2_s

DFINETransformer:
  dim_feedforward: 512
  pg_enabled: True
  pg_hidden_dim: 32
  pg_scale: 1.0

HybridEncoder:
  enable_cas_predictor: True
  use_cass: True
